# 2026-06-04

- 继续开发 Flutter 阅读器项目 squartor：梳理现有书架、阅读器、设置、统计与导入逻辑。
- 新增 BookEntry.lastReadAt 持久化字段；阅读进度更新时写入最近阅读时间。
- 阅读中页现在按最近阅读时间排序，只展示有阅读进度的最近阅读列表，并在继续阅读卡片/最近阅读项中显示最近阅读时间与进度。
- 使用本地 Flutter SDK `D:\antigravity_projects\flutter\bin\flutter.bat` 完成 `flutter analyze` 与 `flutter test`，均通过。
- 按用户提供的 MuMu 安装目录 `D:\Program Files\Netease\MuMu\nx_main`，使用其中 `adb.exe` 连接设备 `127.0.0.1:16384`，构建 debug APK 并安装启动到 MuMu；包名 `com.squartor.reader`，前台窗口确认为 `com.squartor.reader/com.squartor.reader.MainActivity`，截图保存为 `mumu-squartor-home.png`。
- 针对用户反馈“启动加载好几秒、切换页面约 1 秒”做性能优化：应用启动不再 await 状态加载；BookRepository 缓存 SharedPreferences 并一次性读取快照；主页切换改为惰性页面缓存 + Offstage/TickerMode；移除底部导航自定义图标动画；封面按显示尺寸解码；为 MuMu 构建 android-x64 release APK，冷启动 `am start -W` 从通用 release 约 9.4s 降到约 6.8s。`flutter analyze` 与 `flutter test` 均通过，x64 release 已安装到 MuMu。
- 用户质疑“没变化”后，在书架页添加可见构建标记 `_BuildBadge`（性能优化版 · x64 · 时间戳），并发现 Android 构建链存在严重兼容性问题：`file_picker` 在 AGP 9+ 且 `android.builtInKotlin=false` 时错误地跳过 Kotlin 插件，导致 Kotlin 源码无法编译；`flutter_inappwebview_android` 使用已废弃的 `proguard-android.txt`。修复：修改 `file_picker` build.gradle 始终显式应用 `org.jetbrains.kotlin.android`；修改 `flutter_inappwebview_android` build.gradle 改用 `proguard-android-optimize.txt`。重新构建 debug APK 并成功安装到 MuMu，截图 `mumu-squartor-home-v3.png` 确认可见标记已显示。
